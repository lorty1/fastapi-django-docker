default:
  image:  python:3.7
  only:
    - production
  tags:
    - test
  script:
    - sudo docker build web
    - sudo docker run -d -p 8000:8000 web
# image: "python:3.7"
# testrtml
# before_script:
#   - python --version
#   - pip install -r ./core/api/requirements.txt

# stages:
#   - FlakeControl
#   - TestApi
#   - endtest

# lint:
#   stage: FlakeControl
#   script:
#     -  flake8 ./core/api

# testAPI:
#   stage: TestApi
#   script:
#   - pytest ./core/api/

# PRODUCTION:
#   stage: endtest
#   script:
#     -  python --version
